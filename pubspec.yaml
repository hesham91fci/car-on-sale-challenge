name: car_on_sale_challenge
description: "Car on sale challenge"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: 1.3.0
  rxdart: 0.28.0
  flutter_bloc: 9.1.0  

dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
